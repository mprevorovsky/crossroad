/**
 * Geographic directions are implemented as an Enum class.
 * The aim is to minimize errors (compared to a String implementation of directions)
 */
enum class Directions {
    NORTH, SOUTH, WEST, EAST
}